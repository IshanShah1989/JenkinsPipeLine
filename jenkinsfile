node {
	stage ('SCM checkout'){
		git "https://github.com/IshanShah1989/JenkinsPipeLine"
		}
	stage ('Build'){
    	dir("jenkinsPipeline") {
	   sh "mvn clean install"
       }
       	dir("jenkinsPipeline/target") {
	   sh "mvn test"
       }
		}
}